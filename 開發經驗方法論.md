# jingdu 開發經驗方法論

> 用途：下次做「多頁靜態站 + 瀏覽器端 AI/語音/雲端 API」類軟件時的踩坑清單與鐵律。
> 每一條都對應本專案真實發生的事故（2026-07-04 上線 ～ 2026-07-30），不是泛泛而談。
> 建檔 2026-07-30。

---

## 頭號教訓：靜態多頁站沒有「單一真源」，同一件事改一次要記得改 N 遍

**事故（反覆發生，不只一次）**：
- 加一個新學習環節，同步點要改 **10 處**：雙語言引擎 JS ×2、自建課 viewer 模板 ×2、已生成的靜態課文頁 ×3、生成腳本模板 ×2、目錄頁 secs 定義 ×2。
- 加「吸頂控制列」CSS，漏改了「已經生成好的 3 個課文頁」——它們是**手寫/生成後的靜態 HTML**，不吃 viewer 模板，改了模板等於沒改。
- 把「上一句/下一句」移到「聽一遍」兩側，因為這排按鈕是**靜態 HTML 跨 7 個檔案共用同一段結構**（3 個課文頁 + 2 個 viewer 模板 + 2 個生成腳本模板），一次要改 7 遍，漏一個就有一個地方沒生效。
- 振假名解析寫了**兩套正則**（`toKana` 用一套、`collectKanjiMap` 自己另寫一套），兩套行為不一致，其中一套把識別文字整段替換錯，導致「念對了也打不到分」的真機 bug，隱藏了好幾輪才被抓到。
- `help.html` 教學文檔**反覆**在做完一輪新功能後忘記同步更新，被用戶提醒兩次以上。

**根因**：這是純靜態站，沒有後端模板繼承/組件化，「同一段 UI/邏輯」天然會被複製貼上到多個檔案裡（每課一份 HTML、雙語言各一份 JS、模板 vs 已生成內容分離）。改一個地方，人腦很容易忘記「這段東西其實還存在於別的地方」。

**鐵律**：
1. **改動前先問「這段 UI/正則/邏輯，還存在於哪些檔案裡？」** 用 `grep -rn` 找同一個函式名/選擇器/固定字串，列出全部命中檔案再動手，不要改完第一個就以為完了。
2. **能寫成「引擎在執行期只插入一次」的動態邏輯，就別寫成「N 份靜態模板各放一份」**。這次「上一句/下一句移到 pills 下方」就是靠 JS 在 runtime 插入一次（`injectSecNav`），插入後不隨每次重繪消失，比起動 7 個靜態檔案更穩、以後也不用再同步。**只有「用戶明確要求特定視覺位置且和另一顆靜態按鈕綁在一起」時才不得不動靜態模板**（本例的「聽一遍」兩側就是這種情況）。
3. **同一件事只能有一套解析/判斷邏輯（單一真源）**。振假名正則只能有一個地方定義（`ruby.js` 的 `kanjiReadings`），其他地方呼叫它，不准自己再寫一次類似的正則——哪怕看起來只是抄一遍，兩套代碼會慢慢長歪。
4. **每做完一輪面向使用者的功能，同一輪就更新 `help.html`**，不要等使用者問「教程更新了嗎」才想起來。

---

## 架構層教訓（比上面所有戰術更上游，立項時就該想清楚）

> 這三條是複盤全程後才看清的「大問題」：很多輪的來回，根子不在某個 bug，而在**一開始選了「純靜態站」這個架構**。純靜態站有它的巨大優點（零運維、GitHub Pages 免費、離線可用），但它的約束會一路貫穿整個開發，事後才發現代價很重。下次立項要先把這筆帳算清楚，再決定值不值得。

### 選型-1. 純靜態站＝不能藏密鑰＝所有 AI 功能被迫 BYOK，這個決定要提前跟用戶講清代價
- **必然後果**：純前端沒有後端可以放 API key，所以每一個 AI 功能（生成課文、雲端語音、發音評估）都只能 **BYOK（bring-your-own-key，用戶自己申請、自己填、費用自負）**。這連帶產生一長串「本來不必有」的工作量：教用戶去哪申請 key、怎麼填、referrer 怎麼鎖防盜用、費用怎麼對帳、免費額度多少、key 存哪安不安全……**這些全是「因為選了純靜態」才多出來的**。
- **鐵律**：立項時就問「這個軟件要不要用 AI / 要不要藏密鑰 / 要不要按用戶收費」。**只要答案有一個是『要』，純靜態站就會讓你和用戶都很累**（用戶要自己搞定 key，你要處理一堆 BYOK 的邊角）。這時要嘛接受 BYOK 的代價並提前跟用戶說清楚「這個軟件需要你自己準備 XX 帳號和 key」，要嘛就該考慮加一個最薄的後端（哪怕只是一個代理密鑰的 serverless function）。**不要默默選了純靜態，然後在開發到一半才發現「原來每個 AI 功能都要用戶自己弄 key」。**

### 選型-2. 純靜態站的「資料」只能在瀏覽器，跨裝置同步/多用戶/家長控制全是硬仗
- **必然後果**：沒有後端資料庫，所有進度/錯題/設定只能存 `localStorage`。於是：跨裝置同步只能**借用戶自己的 git 私有倉當資料庫**（又是一套 BYOK 令牌）；「家長 PIN 鎖」只能是**前端軟鎖**（擋隨手改可以，擋不住會開 devtools 的人）；iOS「加入主畫面」變成獨立 App 後 **localStorage 跟 Safari 隔離**，進度不帶過去，這是 iOS 硬限制繞不過。
- **鐵律**：**「資料要不要跨裝置、要不要多用戶、要不要真正的權限控制」是純靜態站的三個致命問題**。如果這三個都要，純靜態站會讓你用一堆彆扭的變通方案（借 git 倉、前端軟鎖、教用戶重設同步碼）去逼近，永遠做不到「真正對」。立項時如果這三個是核心需求，就別選純靜態。

### 選型-3. 目標平台含 iOS Safari＝音訊/麥克風/PWA 要預留大量真機調試時間，很多是硬限制
- **事故（本專案 iOS 相關的坑佔了全部 bug 的一大半）**：音訊路由 `playback`/`play-and-record` 來回踩、`ScriptProcessorNode` 真機靜音、麥克風權限是會話級每次重開都問、加主畫面後 localStorage 隔離、微信內建 WKWebView 完全不給麥克風權限、iOS 不把最自然的 Siri 神經語音開放給網頁……
- **鐵律**：
  1. **只要目標裝置含 iPhone/iPad，且功能涉及「錄音 / 播放 / 麥克風 / PWA」，就要在排期時預留大量真機調試時間**——這些在桌面瀏覽器和本機測試永遠測不出來，且很多是蘋果的硬限制（不是你代碼的問題，是繞不過）。
  2. **iOS 錄音一律用 `MediaRecorder`，不要用 `ScriptProcessorNode`（真機靜音）或指望 `AudioWorklet`**。
  3. **本機測試綠 ≠ iOS 能用**。涉及 iOS 音訊/麥克風的功能，本機測試只能驗「邏輯對不對」，「真機能不能用」必須靠用戶真機 + 診斷頁閉環。**這類功能不要在無法真機驗證時就當作完成、更不要直接推上線影響核心體驗**（本專案「聽全文接雲端」就是在沒真機驗的情況下硬上，導致 iOS 上沒聲音、連續 regression 好幾輪；正確做法是先做成「實驗開關 + 出問題自動退回系統聲」，穩了再預設開）。

---

## A. 別信任外部輸入的格式，程式碼要強制清洗兜底

**事故**：
- 要求 AI 把對話人名拆進獨立欄位，只寫在 prompt 裡——AI 遵從率不到 100%，人名常常還黏在句子裡，導致跟讀/背句把人名一起讀出來。
- 要求 AI 生成聽力題的正確答案下標——弱模型一次生成整課常把 10 題錯 4 題，甚至題目和依據句錯位。
- 同樣的 API 呼叫，其中一處漏加 `{json:true}`，AI 回應格式不穩，整批解析失敗，觸發保底邏輯把全部正常題目都換成程式化保底題。

**鐵律**：
- **任何「要求 AI 輸出特定格式」的規則，光靠 prompt 是不夠的**，必須在收到結果後用程式碼**強制清洗/校驗**（正則強制拆分人名、下標範圍與型別校驗、答案二次核對、核不出來就丟棄不留半成品）。這是防禦性設計，不是不信任 AI 能力，是不信任「格式遵從率 100%」這個假設。
- **有一個程序化保底路徑**：AI 出的題核對不過，就退回「用純代碼邏輯保證答案 100% 正確」的保底生成（本例是「聽音辨句」——播一句、四選一原句，答案天生正確不靠模型判斷）。內容質量會降，但**正確性永遠有底線**，不會出現「正確答案卻判你錯」這種比沒有這功能還糟的體驗。
- **同一類 API 呼叫如果分散在多處**，改動時要 `grep` 全部呼叫點逐一核對都套用了同一修法，不能改完一兩處就以為全改了——這正是「聽力題全變保底題」那次回歸的根因（唯一漏加 `{json:true}` 的那一處）。

---

## B. 平台/第三方 API 行為要查證或真機驗證，不要靠猜

**事故（反覆發生，代價最高的一類）**：
- 一度斷言「全球版 Azure 一定要翻牆」——是錯的，用戶真機實測香港區不翻牆能用。
- iOS `audioSession.type` 該設 `playback` 還是 `play-and-record`，來回踩了好幾輪（錄音後放音變小聲、放音後錄音收不到聲），才理清「誰用誰設、雙向切換」。
- 錄音用 `ScriptProcessorNode`（已棄用 API），本機測試沒問題，**真機上 iOS 會靜默回傳空音訊**，導致 Azure 一直回「聽到靜音」——換成 `MediaRecorder` 才根治。
- Azure 發音評估 REST 的回應格式，前後猜錯兩次：先漏了 `&format=detailed` 導致沒有分數欄位；改完又猜錯巢狀結構（以為分數在 `NBest[0].PronunciationAssessment.X`，其實 REST v1 是平鋪在 `NBest[0].X`）。兩次都是靠 **WebSearch 查證官方/社群真實回應格式**才修對，猜測沒有一次是對的。

**鐵律**：
1. **涉及第三方雲端 API 的請求/回應格式，先 WebSearch 查證真實範例，不要憑記憶或直覺猜**——AI 廠商的 REST API 版本間差異、SDK vs REST 格式不同，猜錯的成本是一整輪「還是不行」的真機來回。
2. **涉及瀏覽器平台特性（iOS 音訊路由、Web Speech、getUserMedia）的實作，優先查「這個平台這個 API 在真實裝置上是否可靠」的已知問題**（例如 ScriptProcessorNode 在 iOS 上的已知靜音問題），而不是本機（無麥克風/桌面瀏覽器）測試綠了就當作真的能用。
3. **測不了的真機路徑要老實講**，並提供「用戶自己能一鍵驗證」的診斷頁（本專案有 `jp-mic-test.html`、`azure-test.html`），不要假裝已經測過。每次真機回報新錯誤訊息（如 `InitialSilenceTimeout`），那句錯誤訊息本身就是最精準的下一步線索——**系統性除錯：先讀懂錯誤訊息在說什麼，再動手修，不要看到「還是不行」就重複上一輪的猜測**。
4. **武斷斷言之前，想一下這句話是「我驗證過的事實」還是「我以為的常識」**——很多平台限制會隨版本/區域/帳號類型變化，過去對的結論現在不一定對。

---

## C. 快取是最容易被誤判成「代碼沒修好」的假 bug 源頭

**事故**：
- 用戶反覆回報「還是錯的」，改了好幾輪才發現用戶一直用**瀏覽器快取的舊版 JS**，修復從沒真正生效過。
- 「背句秒數都固定 8 秒」——查了老半天代碼邏輯，最後發現代碼是對的（各句確實隨句長變 4~15 秒），純粹是**用戶當時看到的是舊版快取**。

**鐵律**：
1. **靜態站部署後，Service Worker/瀏覽器快取策略要早期做對**（network-first + `cache:'no-cache'` 帶 etag 驗證），讓「普通刷新」就能拿到最新代碼，不要靠用戶每次硬刷新。
2. **加一個肉眼可見的版本戳**（本專案是首頁頁尾「版本 YYYY.MM.DD」），**每次改動後端記得同步 bump**。用戶回報「不對」時，第一步永遠是先確認版本號對不對，再花時間查代碼邏輯——省下大量「代碼明明是對的卻查半天」的來回。
3. **測試環境同樣要小心殘留進程佔用端口**：多個 `http.server` 沒清乾淨會服務錯目錄，出現 `window.X undefined`/404 這類看起來像代碼壞掉、其實是測試環境髒的假象。診斷順序：先 `pkill` 清乾淨、`curl` 確認 200，再懷疑代碼。

---

## D. 會花錢/涉及外部服務的功能，opt-in + 金絲雀對帳，別批量下猜測

**鐵律（本專案執行得不錯，值得延續）**：
1. **新接的付費/會產生外部副作用的功能，預設關閉（opt-in）**，並在功能說明裡把「要花錢嗎、大概多少、怎麼對帳」講清楚（本專案的發音評估、雲端 TTS 皆如此）。
2. **首筆真實用量之後，引導用戶自己去後台核對實際扣費**，不要只憑估算就宣稱「免費」。
3. **知道雲端帳單有延遲**（本例 Azure 費用延遲 8–24 小時），第一次看到「--」不能立刻當作「確定零元」，要提醒用戶隔一段時間再確認一次，並提醒把查詢範圍切到夠廣（訂閱層級 vs 單一資源群組），避免因為範圍太窄漏看真實花費。

---

## E. 測試紀律：自審有確認偏誤，Mock 要誠實，外部審查要定期做

**事故**：
- 早期背句/跟讀的 playwright 測試**全程 mock 語音識別**假裝成功，從未測過真實識別路徑——用戶當面質疑「你到底有沒有建立測試」，才發現這條路唯一能測的只有「判分邏輯」本身，而識別/麥克風那段測不了。
- 一致性測試裡有一條斷言寫成 `A && (B || True)`，等於恆為真的假綠燈，自己審查沒發現，是外部審查（fable-5）用 grep 讀出邏輯才抓到。
- 派 fable-5 做一次「功能 × 測試覆蓋」審查，一次抓出 20 個完全沒有自動測試盯著的功能（雲備份還原、積分防刷、艾賓浩斯畢業邏輯等），這些都是核心邏輯，此前只靠人工偶爾點一點。

**鐵律**：
1. **能測的部分（純函式判分邏輯、請求格式、狀態機、DOM 渲染結果）必須用真實斷言測到閉環，不能用 mock 掩蓋**；mock 只能用在「這條路徑本機真的碰不到」的地方（真麥克風、真付費 API），而且要在測試檔案開頭寫清楚「這裡為什麼要 mock、真實路徑靠什麼閉環」。
2. **每個使用者能感知的功能，都要有一支能重跑的自動測試盯著**——不是「做完看起來能用」就結束，而是要能在下一次改動後一鍵重跑驗證沒壞。本專案最終固化成「一個命令跑全部測試」的 runner，新增測試自動被納入，不用每次手動加清單。
3. **自我審查有天然的確認偏誤**——寫代碼的人很難發現自己邏輯裡的假綠燈。**週期性地派一個獨立的外部審查**（不同的 agent/模型、要求逐項 grep 佐證不能憑印象），比自己再讀一遍代碼更容易抓到系統性缺口。
4. **改視覺 UI 光有功能測試綠燈不夠**，要有量化的視覺回歸驗證（螢幕寬度下 `scrollWidth` 是否溢出、實際截圖看排版），因為「功能測試通過」和「畫面沒被撐爆」是兩件不同的事。

---

## F. 產品/UX 層：功能不是加了就好，要先看有沒有等效功能已存在

**事故**：首頁「今天的學習」動作條和「今日狀態」大卡，是兩個不同時期各自需求驅動加上去的，結果說的其實是同一件事，變成畫面上兩塊重複資訊，是用戶自己發現才提出「是不是重複了」。

**鐵律**：**加一個新的入口/狀態展示前，先掃一遍現有畫面有沒有已經在做類似的事**，不要等使用者發現重複才合併。多個功能各自獨立開發時尤其容易漏這一步，因為每次只看著眼前這個需求，沒有回頭看全局。

---

## G. 版權/合規邊界：使用者施壓也要守住

**事故**：用戶曾在深夜要求「自己批量做完剩下的教材」，但那是有版權的教材全書——批量抓取發布到公開網站等於規模化複製受版權內容。

**鐵律**：**遇到版權/合規類的硬邊界，就算使用者當下施壓也不能因為「省事」或「討好」而妥協**，守住線、只做合規的部分（本例是提供生成器讓使用者自己輸入自有內容），並把原因說清楚。這類判斷不該用「使用者要求了就做」的邏輯處理。

---

## 一句話總表

| 場景 | 鐵律 |
|---|---|
| **架構選型（最上游）** | 要用AI/藏密鑰/按用戶收費→純靜態站被迫BYOK，代價很重要提前算清並跟用戶講 |
| **架構選型** | 資料要跨裝置/多用戶/真權限控制→別選純靜態站（只能借git倉+前端軟鎖硬湊） |
| **架構選型** | 目標含iOS Safari且涉音訊/麥克風/PWA→預留大量真機調試，錄音用MediaRecorder，核心改動別在沒真機驗時硬上 |
| 靜態多頁站改動 | 先 grep 全部同structure的檔案，改一個地方問「還有哪裡有一份」 |
| 能動態插入 vs 靜態模板複製 | 優先讓引擎 runtime 插入一次，別複製到 N 份靜態檔案裡 |
| 同一件事的解析/判斷邏輯 | 只能有一個單一真源，別自己再寫一套類似的 |
| 做完新功能 | 同一輪更新使用說明文檔，別等用戶問 |
| AI/外部輸入的格式 | prompt 只是提示，程式碼要強制清洗校驗兜底 + 有 100% 正確的保底路徑 |
| 同類 API 呼叫分散多處 | 改動要 grep 全部呼叫點逐一核對都套了同一修法 |
| 第三方 API 請求/回應格式 | 先 WebSearch 查證真實範例，別憑記憶猜 |
| 瀏覽器/平台特性 | 查已知問題 + 真機驗證，本機測綠不代表真機能用 |
| 真機測不了的路徑 | 老實講 + 給用戶能自己驗證的診斷頁，別假裝測過 |
| 用戶回報「還是不對」 | 先查版本戳/快取，別急著懷疑代碼邏輯 |
| 花錢/外部副作用功能 | opt-in 預設關 + 金絲雀首筆真實對帳 + 講清帳單延遲 |
| 寫測試 | 能測的用真實斷言測到閉環，mock 只能用在真測不到的地方 |
| 審查代碼品質 | 定期找獨立第三方審查，自審有確認偏誤 |
| 改視覺 UI | 功能測試綠 ≠ 畫面沒撐爆，要截圖/量尺寸驗證 |
| 加新功能/入口 | 先掃現有畫面有沒有已經在做類似的事，別造成重複 |
| 版權/合規邊界 | 使用者施壓也要守住，說清楚為什麼 |
